# 🧹 Полная очистка и чистая установка

## ⚠️ ВНИМАНИЕ!

Эта инструкция **УДАЛИТ ВСЁ**:
- ✅ Все Docker контейнеры Supabase
- ✅ Все данные локальной базы данных
- ✅ node_modules (зависимости npm)
- ✅ .env.local (локальные настройки)

**ДАННЫЕ В БАЗЕ БУДУТ ПОТЕРЯНЫ!**

Если у вас есть важные данные - сначала сделайте бэкап:
```bash
supabase db dump -f backup-$(date +%Y%m%d).sql
```

---

## 🚀 Вариант 1: Автоматическая очистка (РЕКОМЕНДУЕТСЯ)

### Для Windows:

```bash
# Двойной клик на файл:
clean-all.bat

# Или из PowerShell/CMD:
.\clean-all.bat
```

### Для Linux/macOS:

```bash
chmod +x clean-all.sh
./clean-all.sh
```

Скрипт спросит подтверждение перед удалением.

---

## 🔧 Вариант 2: Ручная очистка (пошагово)

Если хотите контролировать каждый шаг:

### Шаг 1: Остановить Supabase

```bash
supabase stop --no-backup
```

**Что делает:** Останавливает все Docker контейнеры Supabase

### Шаг 2: Удалить Docker контейнеры Supabase

**Windows (PowerShell):**
```powershell
docker ps -a --filter "name=supabase" -q | ForEach-Object { docker rm -f $_ }
```

**Linux/macOS:**
```bash
docker ps -a --filter "name=supabase" -q | xargs docker rm -f
```

**Что делает:** Удаляет остановленные контейнеры

### Шаг 3: Удалить Docker volumes (данные БД!)

**Windows (PowerShell):**
```powershell
docker volume ls --filter "name=supabase" -q | ForEach-Object { docker volume rm $_ }
```

**Linux/macOS:**
```bash
docker volume ls --filter "name=supabase" -q | xargs docker volume rm
```

**Что делает:** **УДАЛЯЕТ ВСЕ ДАННЫЕ БАЗЫ!**

### Шаг 4: Удалить node_modules

**Windows:**
```bash
rmdir /s /q node_modules
```

**Linux/macOS:**
```bash
rm -rf node_modules
```

**Что делает:** Удаляет установленные npm пакеты

### Шаг 5: Удалить локальные настройки

**Любая ОС:**
```bash
rm .env.local
# или del .env.local (Windows CMD)
```

**Что делает:** Удаляет файл с локальными переменными окружения

### Шаг 6: (Опционально) Удалить папку supabase/

Если хотите полностью переинициализировать Supabase:

**Windows:**
```bash
rmdir /s /q supabase
```

**Linux/macOS:**
```bash
rm -rf supabase
```

**Что делает:** Удаляет конфигурацию Supabase (миграции останутся в Git)

---

## ✅ Проверка очистки

После очистки проверьте:

```bash
# Проверка Docker контейнеров
docker ps -a --filter "name=supabase"
# Должно быть пусто

# Проверка Docker volumes
docker volume ls --filter "name=supabase"
# Должно быть пусто

# Проверка файлов
ls -la node_modules  # Должна быть ошибка "not found"
ls -la .env.local    # Должна быть ошибка "not found"
ls -la supabase      # Должна быть ошибка "not found" (если удаляли)
```

---

## 🎯 Что делать после очистки?

### 1. Начните с нуля

Откройте [QUICK_START.md](QUICK_START.md) и следуйте разделу "Первый запуск"

### 2. Или следуйте чеклисту

Откройте [CHECKLIST.md](CHECKLIST.md) и выполните все пункты заново

### 3. Кратко (для опытных):

```bash
# 1. Реинициализировать Supabase
supabase init

# 2. Запустить Supabase
supabase start

# 3. Скопировать credentials
# Сохраните API URL и anon key из вывода

# 4. Создать .env.local
copy .env.local.example .env.local
# Отредактировать - вставить URL и ключи

# 5. Применить миграции
supabase db reset

# 6. Установить зависимости
npm install

# 7. Запустить
npm run dev
```

---

## 🐛 Что делать если что-то пошло не так?

### Ошибка: "Cannot remove container"

```bash
# Остановить все контейнеры
docker stop $(docker ps -q)

# Попробовать снова
docker rm -f <container_id>
```

### Ошибка: "Volume is in use"

```bash
# Остановить Docker Desktop полностью
# Запустить заново
# Попробовать удалить снова
docker volume rm <volume_name>
```

### Ошибка: "Permission denied"

**Windows:**
- Запустите PowerShell/CMD от имени администратора

**Linux/macOS:**
```bash
sudo rm -rf node_modules
sudo docker volume rm <volume_name>
```

### Docker Desktop не отвечает

1. Полностью закрыть Docker Desktop
2. Открыть Task Manager (Ctrl+Shift+Esc)
3. Завершить все процессы Docker
4. Запустить Docker Desktop заново
5. Подождать 1-2 минуты

---

## 💾 Как сделать бэкап ПЕРЕД очисткой?

### Бэкап базы данных:

```bash
# Если Supabase запущен
supabase db dump -f backup-before-clean.sql
```

### Бэкап настроек:

```bash
# Скопировать .env.local
copy .env.local .env.local.backup

# Или
cp .env.local .env.local.backup
```

### Восстановление из бэкапа:

```bash
# После развёртывания Supabase заново
psql postgresql://postgres:postgres@localhost:54322/postgres < backup-before-clean.sql
```

---

## 🧹 Глубокая очистка Docker (если нужно освободить место)

**ВНИМАНИЕ!** Удалит ВСЁ в Docker (не только Supabase):

```bash
# Удалить все остановленные контейнеры
docker container prune -f

# Удалить неиспользуемые volumes
docker volume prune -f

# Удалить неиспользуемые образы
docker image prune -a -f

# ИЛИ всё сразу (ОПАСНО!)
docker system prune -a --volumes -f
```

---

## 📊 Сколько места освободится?

Примерно:

- Docker volumes (Supabase): **~500 МБ - 2 ГБ**
- node_modules: **~500 МБ**
- Docker образы Supabase: **~1-2 ГБ**

**Итого: 2-5 ГБ**

---

## ✅ Чеклист очистки

- [ ] Сделан бэкап БД (если нужны данные)
- [ ] Остановлен Supabase (`supabase stop`)
- [ ] Удалены контейнеры (`docker ps -a | grep supabase`)
- [ ] Удалены volumes (`docker volume ls | grep supabase`)
- [ ] Удалены node_modules
- [ ] Удалён .env.local
- [ ] (Опционально) Удалена папка supabase/
- [ ] Проверка: `docker ps -a` - нет контейнеров supabase
- [ ] Проверка: `docker volume ls` - нет volumes supabase
- [ ] Готов к чистой установке

---

## 🎉 После выполнения

Система полностью чиста! Теперь:

1. Откройте **[QUICK_START.md](QUICK_START.md)**
2. Следуйте инструкциям с самого начала
3. Используйте **[CHECKLIST.md](CHECKLIST.md)** для контроля

---

<div align="center">

**Удачной чистой установки! 🚀**

[← Назад к документации](DOCS_INDEX.md)

</div>
