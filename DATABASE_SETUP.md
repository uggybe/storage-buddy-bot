# 🚀 База данных - Инструкция по развертыванию

Этот документ описывает, как развернуть базу данных Storage Buddy Bot на новом Supabase проекте или любой PostgreSQL базе данных.

## 📋 Варианты развертывания

### Вариант 1: Новый Supabase проект (Рекомендуется)

#### Шаг 1: Создайте новый проект
1. Перейдите на [supabase.com/dashboard](https://supabase.com/dashboard)
2. Нажмите **"New Project"**
3. Заполните данные:
   - **Name**: Storage Buddy Bot
   - **Database Password**: Запомните пароль!
   - **Region**: Выберите ближайший к вам регион
4. Нажмите **"Create new project"**

#### Шаг 2: Примените SQL скрипт
1. В Supabase Dashboard откройте **SQL Editor** (левое меню)
2. Нажмите **"New query"**
3. Скопируйте содержимое файла `setup_complete_database.sql`
4. Вставьте в редактор SQL
5. Нажмите **"Run"** (или Ctrl+Enter)
6. Дождитесь выполнения (должно показать "Success. No rows returned")

#### Шаг 3: Обновите .env файл
1. В Supabase Dashboard перейдите в **Settings** → **API**
2. Найдите:
   - **Project URL** (например: `https://abcdefgh.supabase.co`)
   - **anon public** ключ
3. Обновите файл `.env`:

```env
VITE_SUPABASE_PROJECT_ID="ваш_project_id"
VITE_SUPABASE_PUBLISHABLE_KEY="ваш_anon_public_ключ"
VITE_SUPABASE_URL="ваш_project_url"
```

#### Шаг 4: Запустите приложение
```bash
npm install
npm run dev
```

✅ **Готово!** База данных развернута и готова к работе!

---

### Вариант 2: Своя PostgreSQL база данных

#### Требования
- PostgreSQL 13+ с установленным расширением `uuid-ossp`
- Доступ к Supabase Auth (или своя реализация аутентификации)

#### Шаг 1: Подключитесь к базе данных
```bash
psql -h ваш_хост -U ваш_пользователь -d ваша_база
```

#### Шаг 2: Примените скрипт
```bash
psql -h ваш_хост -U ваш_пользователь -d ваша_база -f setup_complete_database.sql
```

#### Шаг 3: Настройте подключение
Обновите конфигурацию Supabase клиента в `src/integrations/supabase/client.ts`

⚠️ **Внимание**: Вариант 2 требует дополнительной настройки аутентификации!

---

## 📊 Структура базы данных

После развертывания у вас будет:

### Таблицы
- **`items`** - Предметы на складе
- **`app_users`** - Пользователи системы
- **`transactions`** - История операций

### Enum типы
- **`warehouse_type`**: 'Мастерская' | 'Холодный' | 'Теплый'
- **`item_type`**: 'единичный' | 'множественный'

### Функции и триггеры
- `update_updated_at()` - Автообновление временных меток
- `handle_new_auth_user()` - Автосоздание профиля при регистрации
- `update_item_current_user()` - Отслеживание пользователя единичного предмета

### Row Level Security (RLS)
✅ Все таблицы защищены RLS политиками
✅ Доступ только для аутентифицированных пользователей
✅ Автоматическое создание профиля при регистрации

---

## 🔄 Миграции

Все миграции находятся в папке `supabase/migrations/`:

1. **20251108195728** - Начальная схема БД
2. **20251108195747** - Исправление безопасности функций
3. **20251108213650** - Настройка аутентификации и RLS
4. **20251108231640** - Добавление типов предметов (единичный/множественный)

Файл `setup_complete_database.sql` объединяет все миграции в правильном порядке.

---

## 🛠️ Устранение неполадок

### Ошибка: "type already exists"
Если вы запускаете скрипт повторно, сначала очистите базу:
```sql
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO postgres;
GRANT ALL ON SCHEMA public TO public;
```

### Ошибка: "permission denied"
Убедитесь, что используете роль с правами создания таблиц (обычно `postgres`).

### База данных пустая после развертывания
Это нормально! Теперь можно:
1. Зарегистрироваться в приложении
2. Добавить первые предметы
3. Начать работу

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Supabase Dashboard → Logs
2. Убедитесь, что все миграции выполнились успешно
3. Проверьте, что .env файл содержит правильные данные

**Удачи с развертыванием! 🎉**
