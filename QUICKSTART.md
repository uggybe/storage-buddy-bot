# ⚡ Storage Buddy Bot - Быстрый старт на Synology

## 🎯 Одна команда для запуска

```bash
./scripts/generate-keys.sh && npm install && npm run build && docker-compose up -d
```

## 📝 Пошаговая инструкция

### 1️⃣ Подготовка (1 раз)

```bash
# Клонируем репозиторий на Synology
ssh admin@your-synology-ip
cd /volume1/docker
git clone https://github.com/uggybe/storage-buddy-bot.git
cd storage-buddy-bot

# Генерируем ключи и конфигурацию
./scripts/generate-keys.sh

# Редактируем .env.local
# ВАЖНО: Добавьте TELEGRAM_BOT_TOKEN и проверьте IP адреса
nano .env.local
```

### 2️⃣ Сборка фронтенда

```bash
# Устанавливаем зависимости
npm install

# Собираем приложение
npm run build
```

### 3️⃣ Запуск

```bash
# Запускаем все сервисы
docker-compose up -d

# Или используйте удобный скрипт
./scripts/start.sh
```

### 4️⃣ Проверка

Откройте в браузере:
- **Приложение**: http://your-synology-ip
- **Админ панель**: http://your-synology-ip:3000

## 🎛️ Основные команды

```bash
# Запуск
./scripts/start.sh

# Остановка
./scripts/stop.sh

# Перезапуск
./scripts/restart.sh

# Просмотр логов
./scripts/logs.sh

# Создание бэкапа
./scripts/backup-local.sh
```

## 🔧 Что нужно настроить в .env.local

**Обязательно:**
```bash
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
SUPABASE_PUBLIC_URL=http://192.168.1.100:8000  # ваш IP
SITE_URL=http://192.168.1.100                   # ваш IP
VITE_SUPABASE_URL=http://192.168.1.100:8000    # ваш IP
```

**Опционально (для email):**
```bash
SMTP_HOST=smtp.gmail.com
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

## 📦 Структура проекта

```
storage-buddy-bot/
├── docker-compose.yml          # Оркестрация сервисов
├── nginx.conf                  # Конфигурация веб-сервера
├── .env.local                  # Ваша конфигурация
├── telegram-service/           # Telegram API сервис
├── scripts/                    # Полезные скрипты
│   ├── start.sh               # Запуск
│   ├── stop.sh                # Остановка
│   ├── restart.sh             # Перезапуск
│   ├── logs.sh                # Логи
│   ├── backup-local.sh        # Бэкап
│   └── generate-keys.sh       # Генерация ключей
├── MIGRATION.md               # Полная документация
└── SELFHOSTED.md              # Документация для self-hosting
```

## 🐛 Решение проблем

### Порты заняты

```bash
# Проверьте занятые порты
sudo netstat -tulpn | grep -E '(80|443|3000|5432|8000)'

# Измените порты в docker-compose.yml если нужно
```

### Не запускается база данных

```bash
# Проверьте логи
docker-compose logs postgres

# Перезапустите
docker-compose restart postgres
```

### Ошибки сборки

```bash
# Очистите и пересоберите
rm -rf node_modules dist
npm install
npm run build
```

## 💰 Стоимость

**БЕСПЛАТНО!** 🎉

Вы больше не платите:
- ❌ Vercel - $0/месяц (было платно при росте трафика)
- ❌ Supabase - $0/месяц (было $25+ при росте БД)

Вы платите только:
- ✅ Электричество для Synology (~$2-5/месяц)
- ✅ Интернет (который у вас уже есть)

## 📚 Подробная документация

Смотрите:
- [MIGRATION.md](./MIGRATION.md) - полное руководство по миграции
- [SELFHOSTED.md](./SELFHOSTED.md) - документация self-hosting

## 🎉 Готово!

Теперь у вас полностью self-hosted Storage Buddy Bot!

**Не забудьте:**
1. ✅ Настроить автоматические бэкапы (cron)
2. ✅ Настроить HTTPS для безопасности
3. ✅ Ограничить доступ через firewall
4. ✅ Регулярно обновлять систему

---

**Вопросы?** → Создайте issue в репозитории
