# ⚡ Быстрый старт на Windows 10

## 🎯 Что нужно один раз установить:

1. **Docker Desktop** - https://www.docker.com/products/docker-desktop/
2. **Node.js** (LTS) - https://nodejs.org/
3. **Git** - https://git-scm.com/download/win
4. **ngrok** (для Telegram) - https://ngrok.com/download

---

## 🚀 Первый запуск (выполняется ОДИН РАЗ)

### 1. Открыть PowerShell/CMD в папке проекта

```bash
cd путь\к\storage-buddy-bot
```

### 2. Установить глобальные инструменты

```bash
npm install -g supabase
```

### 3. Инициализировать Supabase

```bash
supabase init
```

### 4. Запустить Supabase первый раз

```bash
supabase start
```

**ВАЖНО!** Сохраните вывод команды в блокнот:
```
API URL: http://localhost:54321
anon key: eyJhbGci... (длинный ключ)
```

### 5. Создать .env.local файл

```bash
copy .env.local.example .env.local
```

Откройте `.env.local` в блокноте и вставьте:
- `VITE_SUPABASE_URL` → `http://localhost:54321`
- `VITE_SUPABASE_PUBLISHABLE_KEY` → скопируйте `anon key` из шага 4

### 6. Применить миграции базы данных

```bash
supabase db reset
```

### 7. Добавить себя в whitelist

Откройте http://localhost:54323 (Supabase Studio)

SQL Editor → выполните:

```sql
INSERT INTO telegram_whitelist (telegram_id, first_name, last_name)
VALUES (ВАШ_TELEGRAM_ID, 'Имя', 'Фамилия');
```

**Как узнать Telegram ID?**
- Напишите боту @userinfobot

---

## 🔥 Каждый день для запуска

### Вариант 1: Автоматический запуск (РЕКОМЕНДУЕТСЯ)

Двойной клик на файл:
```
start-windows.bat
```

Выберите пункт `1` (Запустить ВСЁ)

### Вариант 2: Ручной запуск

#### Терминал 1 - Запустить Docker Desktop
1. Откройте Docker Desktop
2. Подождите пока запустится

#### Терминал 2 - Запустить Supabase
```bash
cd storage-buddy-bot
supabase start
```

#### Терминал 3 - Запустить Frontend
```bash
cd storage-buddy-bot
npm run dev
```

#### Терминал 4 - Запустить ngrok (для Telegram)
```bash
ngrok http 8080
```

Скопируйте HTTPS URL (например `https://abc123.ngrok.io`)

---

## 📱 Настройка Telegram Bot (ОДИН РАЗ)

1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте `/mybots`
4. Выберите вашего бота
5. `Bot Settings` → `Menu Button` → `Edit Menu Button URL`
6. Вставьте ngrok URL: `https://abc123.ngrok.io`

---

## ✅ Проверка что всё работает

| Сервис | URL | Что должно быть |
|--------|-----|-----------------|
| Supabase Studio | http://localhost:54323 | Админка БД |
| Frontend (локально) | http://localhost:8080 | Приложение |
| Frontend (публично) | https://ваш-ngrok.ngrok.io | Приложение через ngrok |
| Telegram Mini App | Через вашего бота | Открывается приложение |

---

## 🛑 Остановка

### Вариант 1: Автоматический
Двойной клик на:
```
stop-windows.bat
```

### Вариант 2: Ручной
```bash
supabase stop
```

И закройте все терминалы (Ctrl+C)

---

## 🐛 Проблемы?

### Docker не запускается
- Откройте Docker Desktop и подождите
- Проверьте что WSL2 включён

### Порт 54321 занят
```bash
supabase stop
supabase start
```

### Frontend не подключается к БД
- Проверьте `.env.local`
- URL должен быть `http://localhost:54321`
- anon key должен совпадать с выводом `supabase start`

### Telegram Mini App не открывается
- Проверьте что ngrok запущен
- URL в @BotFather должен быть HTTPS
- Перезапустите Telegram

---

## 💾 Резервная копия

### Создать бэкап
```bash
supabase db dump -f backup.sql
```

### Восстановить
```bash
psql postgresql://postgres:postgres@localhost:54322/postgres < backup.sql
```

---

## 🎓 Полная инструкция

Смотрите `DEPLOYMENT.md` для детальных объяснений.

---

## 💰 Стоимость

- ✅ Supabase (self-hosted) - **БЕСПЛАТНО**
- ✅ Docker - **БЕСПЛАТНО**
- ✅ Node.js - **БЕСПЛАТНО**
- ✅ ngrok (базовый план) - **БЕСПЛАТНО**

**ИТОГО: 0₽ в месяц** 🎉

---

## 📞 Нужна помощь?

- Supabase: https://supabase.com/docs/guides/cli
- ngrok: https://ngrok.com/docs
- Telegram Bots: https://core.telegram.org/bots
